name: happyhealth_diagnostics
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  google_fonts: ^6.1.0
  iconforest_teenyicons: ^1.0.0+1
  provider: ^6.1.1
  table_calendar: ^3.0.0
  flutter_datetime_picker: ^1.5.1
  intl: ^0.17.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
